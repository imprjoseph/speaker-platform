{
  "name": "speaker-platform",
  "version": "0.1.0",
  "description": "講者協作與會務追蹤平台 - Google Apps Script + Google Sheets MVP",
  "private": true,
  "scripts": {
    "login": "clasp login",
    "create": "clasp create --type webapp --title \"Speaker Platform\" --rootDir src",
    "push": "clasp push",
    "watch": "clasp push --watch",
    "open": "clasp open",
    "deploy": "clasp deploy",
    "logs": "clasp logs"
  },
  "devDependencies": {
    "@google/clasp": "^2.4.2"
  }
}
